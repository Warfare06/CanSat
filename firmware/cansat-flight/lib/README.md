# Libraries

This directory is for project-specific (private) libraries.

All dependencies are managed via `platformio.ini` using the `lib_deps` directive.
PlatformIO will automatically download and manage them.

If you need to add a custom library, place it in a subdirectory here.
See: https://docs.platformio.org/en/latest/librarymanager/
